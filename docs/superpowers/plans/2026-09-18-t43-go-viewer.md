# T43 时间线查看器 v1（Go 滩头）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 只读查看器单 exe——会话列表 → 单会话时序图（token 柱/事件标记/窗口带/TTL 存活阴影/拖动回放）+ 反跑面板。ADR-0003 的 Go 滩头，为心跳窗口审查与"证明先于实施"服务。

**Architecture:** Go 单模块（零第三方依赖），`go:embed` 嵌入原生 web 前端（手写 SVG，不用图表库——Ruling V1）；后端三包：ledger（读账本 JSONL）/ policy（公式移植+黄金数字锚定）/ server（HTTP+JSON API+反跑仿真）；数据目录发现 `--data` > `FERRYMAN_DATA` > `~/ferryman`。

**Tech Stack:** Go 1.23 标准库；前端原生 HTML+JS+CSS。

**Spec:** 设计文档 §3.7（时间线查看器与反跑）、§3.6（反跑仿真规则：首跳 T0+τ、每 τ、停于 min(窗末, T0+cap)）、ADR-0003。

## Global Constraints

- **只读铁律**：查看器对 `~/ferryman/**` 只有读权限，代码中不得出现任何写/删账本路径（终审将核查）。
- **绑定铁律**：只监听 127.0.0.1；随机端口（--port 可固定）；无 token（只读+本机+临时进程三条件豁免）。
- **隐私红线**：前端与 API 永不返回消息内容，只返回账本字段（本来就只有数字与类型）。
- **公式单源意识**：policy 包是 Python `policy.py` 的移植副本——黄金数字测试锚定同一批实验数（τ=480s、cache_read=25.5、per_beat=26.22、expire=103.5、cap≈1428s）；将来 Python 公式改动必须同步本包（ADR-0003 同步规则）。
- **P_cache 缺省语义**：反跑参数里 `p_cache<=0` → 拒绝推导并返回明确错误（与 Python NoCachePriceError 同义：宁可不算不造数）。
- Go 代码过 `go vet ./...`；每任务 `go test ./...` 全绿；提交信息中文、结尾带 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`。
- 仓库根 .gitignore 追加 `viewer/*.exe` 与 `viewer/ferryman-timeline`（Task 1 内完成）。

---

### Task 1: Go 脚手架 + 账本读取包

**Files:**
- Create: `viewer/go.mod`、`viewer/main.go`（最小骨架：flags+embed 占位+http.ListenAndServe）
- Create: `viewer/internal/ledger/ledger.go`、`viewer/internal/ledger/ledger_test.go`
- Create: `viewer/internal/ledger/testdata/sample.jsonl`、`viewer/internal/ledger/testdata/corrupt.jsonl`
- Modify: 仓库根 `.gitignore`

**Interfaces:**
- Produces（后续任务依赖的精确签名）:
  - `ledger.Load(dir string) ([]Entry, error)`——读 dir 下按名排序的全部 `*.jsonl`，逐行 json 解码，损坏行跳过并计入 `LoadReport.Corrupt`（v1 允许 Load 返回 (entries, error)，损坏计数入 error 前的 log.Printf 即可）。
  - `type Entry struct`（扁平全字段，见下）
  - `type SessionSummary struct` + `ledger.Summarize(entries []Entry) []SessionSummary`——按 lineage_id 聚合。

```go
package ledger

// Entry 是账本一行（扁平结构：字段按 kind 缺省零值）。
type Entry struct {
	V         int     `json:"v"`
	Kind      string  `json:"kind"`
	Ts        float64 `json:"ts"`
	TsISO     string  `json:"ts_iso"`
	Agent     string  `json:"agent"`
	SessionID string  `json:"session_id"`
	LineageID string  `json:"lineage_id"`
	Project   string  `json:"project"`

	// usage
	Model               string `json:"model"`
	Title               string `json:"title"`
	InputTokens         int64  `json:"input_tokens"`
	CacheReadTokens     int64  `json:"cache_read_tokens"`
	CacheCreationTokens int64  `json:"cache_creation_tokens"`
	OutputTokens        int64  `json:"output_tokens"`
	Offset              int64  `json:"offset"`

	// window
	OpenedTS    float64 `json:"opened_ts"`
	ClosedTS    float64 `json:"closed_ts"`
	DurS        float64 `json:"dur_s"`
	PrefixTokens int64  `json:"prefix_tokens"`
	CloseReason string  `json:"close_reason"`

	// handoff / beat / block / inject / bypass
	Provider         string  `json:"provider"`
	Outcome          string  `json:"outcome"`
	PromptTokens     int64   `json:"prompt_tokens"`
	CompletionTokens int64   `json:"completion_tokens"`
	WallS            float64 `json:"wall_s"`
	Tokens           int64   `json:"tokens"`   // inject
	IdleS            float64 `json:"idle_s"`   // block
	Hit              bool    `json:"hit"`      // beat
	CostPred         float64 `json:"cost_pred"`
	CostActual       float64 `json:"cost_actual"`
}

// SessionSummary 是列表页一行。
type SessionSummary struct {
	LineageID string  `json:"lineage_id"`
	Title     string  `json:"title"`
	Project   string  `json:"project"`
	Agents    string  `json:"agents"`   // 去重后逗号连缀（"cc" / "cc,codex"）
	FirstTS   float64 `json:"first_ts"`
	LastTS    float64 `json:"last_ts"`
	Input     int64   `json:"input"`
	CacheRead int64   `json:"cache_read"`
	Creation  int64   `json:"cache_creation"`
	Output    int64   `json:"output"`
	Requests  int     `json:"requests"`  // usage 行数
	Windows   int     `json:"windows"`
	Beats     int     `json:"beats"`
	Handoffs  int     `json:"handoffs"`
	Blocks    int     `json:"blocks"`
}
```

`Load`：`os.ReadDir` → 过滤 `.jsonl` → 逐行 `json.Unmarshal` 到 Entry；解析失败 `continue` 并 log。`Summarize`：遍历一次，map[lineage]内部聚合结构；usage 行累加四类 token、Title 取**最后一个非空**、FirstTS/LastTS 取全 kind 最小/最大 ts；输出按 LastTS 倒序。

- [ ] **Step 1** `cd viewer && go mod init ferryman/viewer`（go 1.23）
- [ ] **Step 2** 写 `internal/ledger/testdata/sample.jsonl`（手造 ≥12 行：3 个 lineage；各含 usage×3/2/1、window×1、handoff×1、beat×1、block×1——字段照 Entry 结构造，ts 用递增小数）
- [ ] **Step 3** 写 `testdata/corrupt.jsonl`（2 行合法 + 1 行 `{broken` + 1 行空行）
- [ ] **Step 4** 写失败测试 `ledger_test.go`：表驱动——Load(sample) 得 12 条、kind 分布正确；Load(corrupt) 得 2 条不报错；Summarize 聚合数（requests/tokens/标题末值/倒序）
- [ ] **Step 5** 实现 ledger.go 使测试绿
- [ ] **Step 6** main.go 最小骨架（编译通过即可）：

```go
package main

import (
	"flag"
	"fmt"
	"log"
	"net"
	"net/http"
)

func main() {
	data := flag.String("data", "", "账本数据目录（默认 $FERRYMAN_DATA 或 ~/ferryman）")
	port := flag.Int("port", 0, "监听端口（0=随机）")
	flag.Parse()
	dir := resolveDataDir(*data)
	ln, err := net.Listen("tcp", fmt.Sprintf("127.0.0.1:%d", *port))
	if err != nil { log.Fatal(err) }
	fmt.Printf("时间线查看器: http://%s （数据目录 %s，Ctrl+C 退出）\n", ln.Addr(), dir)
	log.Fatal(http.Serve(ln, http.NotFoundHandler())) // Task 3 换真路由
}

func resolveDataDir(flagVal string) string {
	if flagVal != "" { return flagVal }
	if env := osGetenv("FERRYMAN_DATA"); env != "" { return env }
	return homeFerryman() // os.UserHomeDir() + "/ferryman"
}
```
（osGetenv/homeFerryman 直接用 os.Getenv/os.UserHomeDir 内联，上面是为排版伪码——实现时直接写平。）
- [ ] **Step 7** 根 .gitignore 追加两行 `viewer/*.exe`、`viewer/ferryman-timeline`
- [ ] **Step 8** `go vet ./... && go test ./...` 全绿 → 提交 `feat(viewer): T43 脚手架+账本读取包——Load/Summarize/损坏行容错`

---

### Task 2: 公式移植包（黄金数字锚定）

**Files:**
- Create: `viewer/internal/policy/policy.go`、`viewer/internal/policy/policy_test.go`

**Interfaces:**
- Produces: `policy.Params`、`policy.Result`、`policy.Derive(Params) (Result, error)`、`policy.SimulateBeats(t0, windowEnd float64, r Result) []float64`、`policy.DoNothingCost(durS float64, r Result) float64`。Task 3 反跑依赖。

```go
package policy

import "errors"

// Params 是一次推导/反跑的输入。价格与 per 同单位制（如 GLM 积分系数 6.9/1.7/24，per=10000）。
type Params struct {
	PIn, PCache, POut float64
	Per               float64 // 单价对应的 token 块大小；<=0 视为 1
	PrefixTokens      float64 // S
	TTLS              float64 // 实测缓存寿命（秒）
	Safety            float64 // τ 系数，默认 0.8
	BeatOutTokens     float64 // 单跳预留输出 token，默认 300
	MaxWaitS          float64 // 手动上限；<=0 = auto
}

type Result struct {
	TauS         float64 `json:"tau_s"`
	CacheRead    float64 `json:"cache_read_cost"` // S×P_cache
	PerBeat      float64 `json:"per_beat"`
	Expire       float64 `json:"expire"` // S×P_in
	CapS         float64 `json:"cap_s"`
}

func Derive(p Params) (Result, error) {
	if p.PCache <= 0 {
		return Result{}, errors.New("p_cache 缺省：无缓存经济，拒绝推导（宁可不算不造数）")
	}
	per := p.Per
	if per <= 0 { per = 1 }
	safety := p.Safety
	if safety <= 0 { safety = 0.8 }
	beatOut := p.BeatOutTokens
	if beatOut <= 0 { beatOut = 300 }
	tau := safety * p.TTLS
	cr := p.PrefixTokens / per * p.PCache
	pb := cr + beatOut/per*p.POut
	ex := p.PrefixTokens / per * p.PIn
	cap := tau * (ex - cr) / pb
	if p.MaxWaitS > 0 && p.MaxWaitS < cap { cap = p.MaxWaitS } // 手动只能往下收
	return Result{TauS: tau, CacheRead: cr, PerBeat: pb, Expire: ex, CapS: cap}, nil
}

// SimulateBeats 按 §3.6 定案排跳：首跳 T0+τ，此后每 τ，停于 min(窗末, T0+cap)。
func SimulateBeats(t0, windowEnd float64, r Result) []float64 {
	var out []float64
	end := t0 + r.CapS
	if windowEnd < end { end = windowEnd }
	for t := t0 + r.TauS; t <= end+1e-9; t += r.TauS {
		out = append(out, t)
	}
	return out
}

// DoNothingCost 什么都不做的窗口成本：整窗 ≤TTL 存活为 0；否则过期一次全款。
func DoNothingCost(durS float64, ttlS float64, r Result) float64 {
	if durS <= ttlS { return 0 }
	return r.Expire
}
```

**黄金测试（必须逐数锚定，注释里写手算链）**：

```go
func TestGoldenGLM(t *testing.T) {
	p := Params{PIn: 6.9, PCache: 1.7, POut: 24, Per: 10000,
		PrefixTokens: 150000, TTLS: 600, Safety: 0.8, BeatOutTokens: 300}
	r, err := Derive(p)
	if err != nil { t.Fatal(err) }
	// 150000/10000=15 块；15×1.7=25.5；25.5+300/10000×24=26.22；15×6.9=103.5；
	// 480×(103.5−25.5)/26.22≈1428.0
	assertF(t, r.TauS, 480); assertF(t, r.CacheRead, 25.5)
	assertF(t, r.PerBeat, 26.22); assertF(t, r.Expire, 103.5)
	if r.CapS < 1427 || r.CapS > 1429 { t.Fatalf("cap=%v", r.CapS) }
}

func TestManualCapOnlyLowers(t *testing.T) { /* MaxWaitS=900 → CapS==900；MaxWaitS=99999 → cap 仍≈1428 */ }
func TestNoCachePriceRefuses(t *testing.T) { /* PCache=0 → err 含 "拒绝推导" */ }
func TestSimulateAndDoNothing(t *testing.T) {
	// τ=480,cap≈1428：t0=0,windowEnd=1500 → 跳在 480,960（1428 截住第三跳）
	// t0=0,windowEnd=700 → 只有 480；dur=700>600 → DoNothing=Expire；dur=500 → 0
}
```
（assertF 用 math.Abs(x-want)<1e-6 自写小助手。）

- [ ] TDD 全程（测试先失败→实现→绿）；`go vet ./... && go test ./...` → 提交 `feat(viewer): T43 公式移植包——Derive/Simulate/DoNothing+GLM黄金数字锚定`

---

### Task 3: HTTP 服务 + JSON API + 反跑

**Files:**
- Create: `viewer/internal/server/server.go`、`viewer/internal/server/server_test.go`
- Modify: `viewer/main.go`（真路由+embed web 占位+自动开浏览器）

**Interfaces:**
- Consumes: Task 1 `ledger.Load/Summarize`、Task 2 `policy`。
- Produces: `server.New(dataDir string) *Server`；路由：
  - `GET /api/sessions` → `{"sessions": []ledger.SessionSummary, "generated_at": <ts>}`
  - `GET /api/timeline?lineage=<id>` → `{"requests":[Entry...按ts升序,kind==usage], "events":[Entry...非usage按ts升序], "windows":[Entry...kind==window]}`
  - `POST /api/backtest` body `{"lineage":"...","opened_ts":123,"closed_ts":456,"prefix_tokens":150000,"params":{"p_in":6.9,"p_cache":1.7,"p_out":24,"per":10000,"ttl_s":600,"safety":0.8,"beat_out_tokens":300,"max_wait_s":0}}` → `{"ok":true,"result":policy.Result,"beats":[t...],"beats_cost":n,"do_nothing_cost":n,"note":""}` 或 `{"ok":false,"error":"..."}`（p_cache≤0 → ok:false + 拒绝推导文案）
  - `/` 起静态文件（`//go:embed web` → embed.FS，Task 4 填充；本任务先放占位 index.html 写着 "T43 web 占位"）

实现要点：
- 每次请求**现读账本**（Load 是幂等只读；6.6 万行量级解析 ~百毫秒，可接受；不做缓存）。
- Server 持有 dataDir；handler 里 `ledger.Load(dir)` 出错 → 500 + 错误文案；数据目录不存在 → `{"sessions":[],"note":"数据目录不存在：<dir>"}` 200。
- timeline 的 lineage 过滤：`e.LineageID == q.Get("lineage")`。
- backtest：从请求取窗口（opened/closed/prefix_tokens 由前端从 window 行带出），params 缺省值与 Task 2 Derive 的缺省规则一致；beats_cost = len(beats)×PerBeat；do_nothing = policy.DoNothingCost(closed-opened, ttl_s, r)。note 里写明 "手动上限只能往下收；cap=auto≈<cap>s"。
- main.go：解析 flags → server.New → `ln, _ := net.Listen("tcp","127.0.0.1:<port>")` → 打印 URL → 非 --no-browser 时按 runtime.GOOS 起浏览器（windows: `cmd /c start <url>`；darwin: `open`；其他 `xdg-open`，exec.Command 忽略错误）→ http.Serve(ln, mux)。

**测试（httptest.NewServer 全表驱动）**：
- testdata 造小型账本目录（tmp）：sessions 聚合数、timeline 三数组过滤正确（requests 只含 usage 且升序）、backtest 黄金参数 → beats=[480,960]（相对 opened_ts 的绝对时间戳）+ beats_cost≈52.44 + do_nothing=103.5（dur=1500>600）、p_cache=0 → ok:false、目录不存在 → 200+空列表。
- `go vet ./... && go test ./...` → 提交 `feat(viewer): T43 HTTP服务+JSON API+反跑仿真端点`

---

### Task 4: Web 前端——会话列表页

**Files:**
- Create: `viewer/web/index.html`、`viewer/web/app.js`、`viewer/web/style.css`

**规格（原生三件套，无框架无构建；hash 路由：`#/`=列表，`#/t/<lineage>`=时序）：**

- `index.html`：`<div id="app"></div>` + `<script src="app.js">`；`<meta charset="utf-8">`；标题"Ferryman 时间线"。
- `app.js` 结构（函数级规格，实现者照此写）：
  - `async function fetchJSON(url, opts)`——fetch+json+错误抛文案。
  - `async function renderList()`——GET /api/sessions → 表格：列=标题（无则 lineage 前 24 字符）、项目目录、开始时间（`fmtTS`：ISO 转本地 `MM-dd HH:mm`）、最后活动、请求数、tokens 合计（input+cache_read+creation，`fmtK`：千分位或 1.2w 格式）、窗口数、心跳数、交接数。行点击 `location.hash = '#/t/'+encodeURIComponent(lineage)`。表头可点排序（v1 只按最后活动倒序即可，排序是加分项不做）。
  - `window.onhashchange = route; function route()`——hash 前缀分发到 renderList/renderTimeline。
  - 顶部固定小工具条：左"Ferryman 时间线"标题、右数据目录说明（从 sessions 响应的 note 带出，若有）。
- `style.css`：暗色可读（背景 #111、字 #ddd、表格斑马纹 #1a1a1a、链接 #6fb3ff、hover 行高亮 #222）；系统字体栈；紧凑（12-13px）。**不引外部资源（无 CDN/字体/图标）**。
- 手工验收：`go run . --data <testdata目录> --no-browser` + curl /api/sessions 出数；浏览器打开列表能渲染（实现者可起 `python -m http.server` 不行——必须走 Go 服务，因为 API 同源）。
- 提交 `feat(viewer): T43 会话列表页——暗色表格+hash路由+fetch渲染`

---

### Task 5: Web 前端——时序图（SVG 手绘核心）

**Files:**
- Modify: `viewer/web/app.js`（大头）、`viewer/web/style.css`

**renderTimeline(lineage) 规格（数据 → 图元映射全部给出）：**

- `GET /api/timeline?lineage=...` 得 `{requests, events, windows}`。
- **布局**（全屏单图 + 底部控制条）：
  - SVG viewBox=`0 0 1200 520`，`width:100%`；顶部 24px 事件泳道区（3 条泳道：beat/handoff、window、gate(block/inject/bypass)）？——简化：**一条事件行 y=12..36**，全部事件符号画在此行，颜色区分；主图 y=60..460 画 token 柱；x 轴刻度 y=470..490。
  - x 映射：`xOf(ts) = 40 + (ts - tMin) / (tMax - tMin) * 1120`；tMin/tMax 取全部元素（含窗口边界）的最小/最大，两侧各留 3% padding（即先算 range 再各扩 3%）。
  - y 映射（token，线性）：`yOf(v) = 460 - v / yMax * 390`；yMax = 所有柱分段合计的最大值向上取整到 10^k×{1,2,5} 阶梯（如 260k→300k）。
- **柱**（每 request 一根，宽 = max(2px, 相邻柱间距的 60%)，居中于 xOf(ts)）：
  - 三段堆叠自底向上：绿 #4caf50 = cache_read_tokens、红 #e57373 = input_tokens+cache_creation_tokens、蓝 #64b5f6 = output_tokens（右端小标签或 tooltip 区分）。
  - hover tooltip（绝对定位 div，跟随鼠标）：时间、模型、input/cache_read/creation/output 四数、标题。
- **事件标记**（事件行 y=24 基线）：
  - beat：绿色圆点 r=4，title 属性 `beat 命中=<hit> 实收=<cache_read> 预测成本=<cost_pred>`
  - handoff：向下小旗（线+三角）#ffb74d，title=`handoff <provider> <outcome>`
  - block：红叉（两条短线）#e57373；inject：上箭头 #4dd0e1；bypass：灰空心点
  - window（来自 windows 数组）：主图背景竖带 rgba(255,255,255,0.06) 从 xOf(opened) 到 xOf(closed)，顶部标签 `等待 <dur_s>s（<close_reason>）`，**点击窗口带选中它**（高亮描边 + 右上角信息卡显示窗口参数，带"反跑此窗口"按钮 → Task 6）。
- **TTL 存活阴影**：默认 ttl 参数 600s（控制条输入框可改）；对每个 refresh 事件（request 与 beat）画从 xOf(ts) 到 xOf(ts+ttl) 的顶部横条 rgba(76,175,80,0.10)（y=40..58 条带，重叠自然加深）——安全垫宽度一眼可见。
- **回放游标**：控制条 `<input type=range>` 全范围（tMin..tMax 步长=range/500）；`oninput` 重渲染：只画 ts ≤ 游标的柱/事件/阴影，右上角实时累计（请求数、tokens 合计）；播放按钮（setInterval 按步推进）可选实现。
- **x 轴刻度**：约 6-8 个等分，标签 `MM-dd HH:mm`。
- 无数据（requests+events+windows 全空）：画居中提示"该族系暂无账本数据"。
- 手工验收：用真实数据 `~/ferryman`（65k 行已回填）打开本会话族系能看到密集柱状+窗口带；截图不必，描述所见即可（柱数≈请求数、窗口带出现）。
- 提交 `feat(viewer): T43 时序图——SVG堆叠柱/事件标记/窗口带/TTL阴影/回放游标`

---

### Task 6: 反跑面板 + 收尾

**Files:**
- Modify: `viewer/web/app.js`、`viewer/web/style.css`
- Create: `viewer/README.md`
- Modify: `viewer/main.go`（如需）

**规格：**
- 窗口信息卡的"反跑此窗口"展开参数表单：p_in/p_cache/p_out/per/ttl_s/safety/beat_out_tokens/max_wait_s（预填 GLM 值 6.9/1.7/24/10000/600/0.8/300/0；prefix_tokens 与 opened/closed_ts 从选中 window 行自动带出只读展示）。
- 提交 → `POST /api/backtest` → 结果渲染在时序图下方结果卡：
  - 三线对比横条：**若当时这样配**（beats_cost，绿色，附"n 跳 @ t+τ…  共 X 积分"）、**实际发生**（若该窗口有真实 beat 行：Σcost_actual；否则显示"—（未启用）"灰色）、**什么都不做**（do_nothing_cost，红色或 0 显示"存活无损"）。
  - 附 cap/τ/单跳/全款四个推导数（来自 result）。
  - `ok:false` 时红字显示拒绝文案（p_cache 缺省场景）。
- README.md：构建（`cd viewer && go build -o ferryman-timeline.exe .`）、运行（默认读 ~/ferryman；--data/--port/--no-browser）、只读声明、公式同步规则一段（引用 ADR-0003：Python policy.py 改动须同步本包+黄金测试）。
- 终验：`go vet ./... && go test ./...` 全绿；`go build` 出 exe；对 `~/ferryman` 冒烟（列表非空、时序可渲染、反跑黄金参数返回 480/960 两跳）。
- 提交 `feat(viewer): T43 反跑面板+README+构建冒烟——查看器v1收口`

---

## Self-Review

1. **Spec 覆盖**：§3.7 图元（柱分段/事件/窗口带/TTL 阴影/预测vs实收/回放/反跑/会话列表标题目录时间）——Task 4/5/6 全落；"永不渲染消息内容"由数据面（账本无内容）+不新增字段双保险。✓
2. **占位符扫描**：Go 代码全量给出；web 任务给到函数级+像素级映射规则，无 TBD。✓
3. **类型一致**：Entry 字段名与账本 JSONL 键逐一对应（v/kind/ts/ts_iso/agent/session_id/lineage_id/project/model/title/input_tokens/cache_read_tokens/cache_creation_tokens/output_tokens/offset/opened_ts/closed_ts/dur_s/prefix_tokens/close_reason/provider/outcome/prompt_tokens/completion_tokens/wall_s/tokens/idle_s/hit/cost_pred/cost_actual）——对照 ferryman/accounts.py `_KIND_FIELDS` 与公共键核对过。✓
