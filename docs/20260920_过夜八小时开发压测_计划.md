# 过夜八小时开发压测：计划与晨间核对单（2026-09-20 夜）

- 目的：验证近期功能（闸门 observe、摆渡、等待窗、T48 判据、子代理入账、SessionStart 归还注入、MCP 面）在日常开发模式下是否成立。
- 方法：真实产品渐进开发模拟——`ft-lab/kvstore`（Go CLI）8 波功能迭代＋`ft-lab/notes-app` 续作；波间闲置时长刻意设计成跨越/不跨越阈值，形成阳性与阴性对照。
- 执行：`ft-lab/soak-runner.sh`（分离进程，日志 `ft-lab/soak-runner.log`）；21:35 起跑，约 03:35 收尾，留 2h 冷却让末批摆渡完成。
- 花费：GLM 套餐 5h 窗口内，用户已授权（"token 很多"）。

## 波次表（阈值：摆渡 25min / 凉 35min）

| 波 | 时刻 | 动作 | 会话形态 | 验证点 |
|---|---|---|---|---|
| W0 | 21:25 | 先导（已发生） | notes-app 整理 A ＋ slow-job 等待 B | B 等待窗已实时确认开窗 ✓；A 摆渡 ~22:00 |
| W0.5 | 22:12 | 恢复 A 会话续提 | resume 已凉会话 | **闸门 observe 告警＋放行**（首个核心验证） |
| W1 | 22:20 | kvstore 引导：CRUD＋持久化＋测试 | 新会话；快/慢子代理各一 | 子代理混合时长；会话入台账 |
| gap | 40min | — | — | 阳性：摆渡触发＋会话变凉 |
| W2 | 23:20 | TTL 功能 | **新会话** | **SessionStart 归还注入**（transcript 应含 INJECT 块） |
| gap | 12min | — | — | **阴性对照：不摆渡不变凉** |
| W3 | 23:47 | 修一个植入 bug | resume W2（未凉） | 闸门静默放行（无告警＝正确） |
| gap | 30min | — | — | 摆渡触发但未到凉线 |
| W4 | 00:27 | 压缩功能＋3 子代理（含 sleep 300 慢任务） | 新会话 | 长等待窗；多子代理计数 |
| gap | 50min | — | — | 长闲置：摆渡＋凉 |
| W5 | 01:37 | benchmark 轮 | resume W4（凉 50min） | observe 告警（第二次）＋交接新鲜 |
| W6 | 01:47 | notes-app 续作 | 新会话（W0 交接已存） | 跨项目注入；账本项目归集 |
| gap | 25min | — | — | 摆渡阈值边缘 |
| W7 | 02:27 | 重构＋release v0.1.0 | 新会话；慢子代理 sleep 240 | 再次等待窗 |
| gap | 35min | — | — | 凉阈值边缘 |
| W8 | 03:17 | 终审：清 ISSUES、写 STATUS | 新会话 | 全项目收口 |

## 晨间核对单（按序执行，5 分钟）

1. `soak-runner.log` 尾部无 ERROR；STATUS.md 两个项目都生成。
2. MCP `heartbeat_status`：昨夜窗口全部闭合，`close_reason` 分布应为 subagents_done 为主。
3. MCP `sessions`（ft-lab 过滤）：末批会话 stale=true。
4. `~/ferryman/handoffs/` 新增按波次对齐：W1/W2(W3 gap)/W4/W6/W7 各一份，**12min gap 的 W2→W3 之间不应有**。
5. W2 会话 transcript `grep -c INJECT` ≥1（归还注入落地）。
6. serve.log：observe 告警行 ≥2（W0.5 与 W5），且均为"已放行"。
7. /stats：usage 行持续增长，子代理转录有入账（四列非零）。

## 已知限制（如实）

- **异步停车窗（T48 saw_async）无法无头模拟**：后台代理随 CC 进程退出而死。需交互会话人工测一次（晨间 10 分钟：派后台子代理后离开 20 分钟，看窗记 expired@1h 还是 main_resumed）。
- 子代理模型别名 `glm-5.3-flash[1m]` 有 unrecognized_model 告警并回退主模型（09-17 已知 cc-switch 老坑，渡口切换后由改写五件根治）——压测按真实现状跑，不规避。
- `/sessions` 输出 cwd 字段为空串（观测面小 bug，不影响闲置判定）——已记，待修。
- 心跳未开（渡口未切，前置不满足）——本压测不含心跳，窗口遥测 beats_fired=0 为正确值。
