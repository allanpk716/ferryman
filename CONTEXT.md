# Ferryman（摆渡人）

本机常驻守护进程：守望各 AI CLI 的会话文件，闲置超时后把旧会话上下文的精华"渡"成交接文档，经提交前钩子拦截凉会话的继续使用，引导开新会话并自动注入交接。缓存死了，摆渡人渡精华。

## Language

### 四个动作

**守望（watch）**：
轮询会话目录的文件变动，维护台账。
_Avoid_: 监控（太泛）、扫描

**摆渡（ferry）**：
对闲置会话执行"读档→提取→调外部模型→写交接 MD"的完整过程；由摆渡执行器完成，从不触碰会话本身。
_Avoid_: 压缩（与 /compact 语义冲突）、总结（仅指模型环节）

**闸门（gate）**：
提交前钩子向守护进程问询"放不放行"的机制；判定只依据台账。
_Avoid_: 拦截器（block 是闸门的一种决定，不是闸门本身）

**归还（restore）**：
新会话启动时自动注入该项目最新交接 MD（连同待续 prompt）。
_Avoid_: 恢复（太泛）、回注

### 会话状态

**凉会话（stale session）**：
闲置时长超过拦截阈值的会话；缓存大概率已失效，继续使用即全量重付。
_Avoid_: 死会话、过期会话

**台账（ledger）**：
session_id 到最后活动时间、上下文大小、标题、项目路径、Agent 类型的登记表；闲置判定的唯一事实源，判定时不解析 jsonl。
_Avoid_: 数据库、状态表

**闲置（idle）**：
当前时刻距台账所记该会话最后一次写入的时长；后台 subagent 与长命令的持续写入都算活动；等答复窗口不改本定义——闲置照走，心跳不算写入。
_Avoid_: 空闲

**会话族系（lineage）**：
resume/继承产生的新会话（新 session_id 或新文件）与原会话的关联；台账沿族系继承闲置史与交接。
_Avoid_: 会话链、父子会话

**拦截阈值**：
判定会话变凉、闸门开始拒绝的闲置时长；默认 35 分钟（E0a 实测拐点 +5min 余量），按 Agent/服务商可配。

**总结阈值**：
触发摆渡的闲置时长；恒不超过拦截阈值，保证不变量"被拦 ⇒ 交接必已存在"；默认 25 分钟。
_Avoid_: 摆渡阈值

**最小上下文门槛**：
低于该规模的会话不值得摆渡也不拦截；默认 20K token 量级。

### 记账

**子代理转录（subagent transcript）**：
会话目录下每个子代理自带的完整对话记录，含每轮官方 usage；判活信号经钩子上报；用量四列随父会话入账（ADR-0008），消息内容永不入账（隐私铁律）。
_Avoid_: sidechain 文件、子会话

**四列口径（usage four columns）**：
input / output / 缓存写 / 缓存读四个官方计数；token 记账的唯一事实源，CLI 自报的汇总数不作账目依据。
_Avoid_: token 总数（单一数字抹掉缓存结构）

**会话总账（session total）**：
一场会话的完整消耗 = 主转录四列加总 + 各子代理转录四列加总；主转录只含主循环自身的请求。
_Avoid_: 会话 token 数（不指明是否含子代理）

**对账（reconciliation）**：
把四列口径算出的账与 CLI 自报数交叉比对、量化偏差的自校验动作；只报告偏差，不修改数据。
_Avoid_: 校准

### 交接

**交接 MD（handoff）**：
摆渡的产物，结构固定：目标 / 已完成与关键结论 / 未完成与下一步 / 关键文件与改动 / 坑与决策 / 续接第一句话。
_Avoid_: 总结文档、快照

**交接库**：
Ferryman 自管的交接 MD 目录与索引；独立于 transcript 的生命周期（transcript 会被 30 天清理）。
_Avoid_: 缓存目录

**待续 prompt（pending prompt）**：
被闸门拦下的用户原话，由守护进程保管，归还时随交接一并注入。
_Avoid_: 被拦消息

**确定性骨架**：
交接中从 jsonl 程序化抽取的事实部分（文件改动、命令、时间线），不经过任何模型。
_Avoid_: 元数据

**模型叙事**：
交接中由语言模型撰写的叙述部分（坑、决策、下一步）；与确定性骨架合成完整交接。

**末段定格（final-exchange freeze-frame）**：
交接中确定性保留的最后一轮对话：普通文本逐字（限额截断）、待选择交互以"问题+选项列表"摘要重述（永不倒交互原文）、结构化片段只给摘要；待选择时续接第一句话即重述该选择。
_Avoid_: 最后回复（含糊——定格的是一轮对话且按形态分流）

**三层漏斗**：
摆渡执行器的输入处理形状：L0 确定性提取过滤 → L1 单次塞入模型 → L2 map-reduce 分块（仅当超出模型窗口）。

**覆盖截止（covers_until）**：
一份交接所涵盖的会话输入边界——稳定快照内最后一条带时间戳记录的时刻（UTC）。
_Avoid_: 总结时间点（那是 created_at）

**有效交接**：
与被拦会话同 Agent 且同项目目录、状态为 fresh 或骨架、覆盖截止不早于台账最后写入的交接；闸门 block 的唯一依据。

**骨架交接（skeleton）**：
仅有确定性骨架、没有模型叙事的降级交接；模型失败、超时或预算耗尽时产出，对闸门仍然有效。
_Avoid_: 残缺交接

**稳定快照**：
摆渡开始时对会话文件的读取边界——两次读到相同字节偏移与相同内容哈希才接受，拒绝半行；覆盖截止的可信来源。

### 闸门语义

**放行（allow）**：
闸门同意该次提交继续，prompt 正常发给模型。

**拦截（block）**：
闸门拒绝该次提交：prompt 被抹除，用户看到原因与交接路径。
_Avoid_: deny（Codex/dsh 的词，语义同 block）、拒绝

**fail-open**：
守护进程不可达、超时或出错时闸门一律放行，与 CC/Codex 钩子原生语义一致。
_Avoid_: fail-closed（明确不做）

**逃生门（bypass）**：
合法绕过闸门的途径：环境变量豁免（自动化保险丝）与 prompt 魔法前缀（单次放行）。
_Avoid_: 白名单

**闸门三态**：
off（不拦）、observe（只警告不拦）、enforce（真拦）；摆渡就绪前闸门只允许 observe。
_Avoid_: 拦截开关

**待交接标记（pending）**：
无有效交接时警告放行一次后记下的状态；其后该会话再次提交即拦截（绕过闲置判定），直到交接就绪或进入新闲置周期——保证逃逸上界为一轮。

**钩子自举（hook bootstrap）**：
钩子发现守护进程未运行时将其无窗口后台拉起；当次提交照旧放行（fail-open 不变）。
_Avoid_: 自启动（太泛）

### 模型与路由

**摆渡路由**：
摆渡执行器调用模型的优先序。定型期目标：本地模型 → 直连 API（CC Switch 层已随 ADR-0012 退役；兜底改走摆渡供应商表内其他条目）；验证期默认云端 DeepSeek V4.1 Flash，最终由评测实验定案。

### 渡口（dock，实施决定 2026-09-19，ADR-0011；多上游直连 2026-09-21，ADR-0012）

**渡口（dock）**：
daemon 内只服务 Claude Code 的 API 中转层：CC 流量经渡口改写后发往上游供应商（渡口上游表直连、active 单选，ADR-0012），心跳快照从渡口入站口原样重放；入站占位令牌不校验、仅绑本机。
_Avoid_: 路由器（与"摆渡路由"撞名）、代理（太泛）

**改写五件**：
渡口对 CC 请求的全部生效改写：模型档位映射（六键）、剥 [1M] 后缀、换上游真钥、text-only 模型的图片降级、出站头卫生。改写集以差分验证为准——出现第六件要么是缺陷要么过 ADR。
_Avoid_: 整流（cc-switch 侧词，仅指其错误恢复件，对本链路 no-op）

**出站头卫生（header hygiene）**：
渡口出站头的处理规则：host 换上游、追踪/CDN 头删、认证类替换为上游真钥、anthropic-beta 重建（保留客户端全部 beta、确保含 claude-code 标记——套餐识别相关）、anthropic-version 透传。
_Avoid_: 头过滤（混同 body_filter——那件对本链路是 no-op）

**形态漂移告警（drift alert）**：
渡口对入站形态的登记与提醒：新出现过的 beta 标记或顶层参数即报——CC 版本升级的第一现场，防静默 miss。

**渡口上游（dock upstream）**：
渡口转发目的地的供应商条目，Anthropic Messages 协议，自带密钥与 model 映射；`[dock].active` 单选。全体条目只收真供应商端点，本地中转地址只作回退通道（守卫强制透传，缺钥豁免）。
_Avoid_: 供应商（不带 lane 限定的口语）、CC Switch 供应商（那是改写 CC 配置的别家概念）

**摆渡供应商（ferry provider）**：
摆渡执行器调来写模型叙事的供应商条目，OpenAI Chat Completions 协议；`[ferry].provider` 单选，与换上游互不联动。
_Avoid_: 渡口上游（协议、字段、生命周期皆不同）

**换上游（upstream switch）**：
人手把 `[dock].active` 从一条渡口上游换成另一条并重启守护的动作（`ferryman upstream use`）；重启即全局作废内存缓存快照，旧会话按冷启动全量重付。纯手动，无故障转移。
_Avoid_: 切换（cutover 一词已专指语言栈往返，见 ADR-0010）、故障转移（明确不做）

**评测实验（E1）**：
以真实历史会话为评测集、对比候选模型交接质量的独立实验；其结果决定摆渡路由的默认顺序。

### 费用账本

**handoff 成本**：
为产出交接 MD 而发生的全部模型调用费用（含分块、重试与失败）。
_Avoid_: 摆渡成本（过程命名，难懂；记账以产物 handoff 命名）

**账本（accounts）**：
逐条记账的 append-only 流水集合，按月滚动；费用与节省判定的唯一事实源，report 只读不改写。只记元数据与金额（时间、会话/族系/项目/Agent、token 数、单价版本、结果码），永不落消息内容。
_Avoid_: 报表、统计数据库

**价格表（price table）**：
config 内 provider+model 到输入/缓存/输出单价的登记，带生效日期与自述单位；每条流水钉死记账时所依据的版本，改价不重算旧账。P_cache 允许缺省——缺省即无缓存经济，策略计算器拒绝推导，report 标注节省额不可算。

**科目（entry kind）**：
流水的分类：handoff / beat / block / inject / bypass / window / usage（逐次请求的用量遥测）/ dock（渡口中转流水：改写前后模型名、四列、延迟、状态码，纯元数据）/ qwatch_hit / qwatch_open / qwatch_close（问询守望命中/开窗/关窗事件，票 04 起入账本科目）。

**成效账（savings statement）**：
report 层按版本化反事实公式算出的节省额；只有真实兑现的避免才计节省，bypass 绕过与无效保温单列。
_Avoid_: 省钱数（无口径的口语）

**等待窗口（wait window）**：
主会话因等待在飞子代理而闲置的起止区间——起于"主会话闲置且存在在飞子代理"，止于**最后一个子代理完成**（主会话自动续跑，无需人在场）、主会话提前恢复写入，或异步等待的停车收尾（见下）；心跳触发、节律与策略对比的分析单元。

_停车（parking，T48；仅 CC 轨，codex 维持旧语义）_：后台异步派发的子代理（工具秒回、真身仍跑几十分钟）会让子代理计数提前归零，这类窗不即闭，改记停表时刻，等主会话恢复调用或过期才收尾。停表未过期的窗是"机器等机器"豁免与摆渡推迟的**第三道判据**（另两道：子代理计数>0、转录尾部有没等到结果的工具调用；三道任一成立即放行/推迟）。是否停车由 `saw_async` 锁存决定——start 与 stop 两处都会读主会话转录尾部判异步（后台参数，或 "Async agent launched" 回执前缀；判据依赖 CC 文案，改版即失灵），判中即置位，此后整窗一律停车语义，"async A 在飞又派 sync B"的交错派发不丢 A 的等待；判不中一律退回旧语义＝即闭——宁少记真窗，不误停假窗。收尾四种（close_reason）：`subagents_done`＝同步派发计数归零即闭（旧语义）；`prompt`＝主会话来讯且窗未停车（「强续」也算恢复写入）；`main_resumed`＝停车窗收到晚于停表 90 秒的用量行（即主会话恢复调用；这 90 秒是 ack 宽限——停表后 90 秒内的用量行视为派发确认回合，不闭窗，只防钩子时序反转）；`expired`＝停车满 PARK_EXPIRE_S（1 小时）懒过期。过期后果（如实）：豁免失效+摆渡可恢复入队——哪怕 async 真身仍在跑，>1h 的异步等待是盲区（2026-09-18 实测最长 34.8 分钟）。已知失真变体：ack 落在宽限内且会话随后静默，该窗记 expired@1h，dur 虚高约 1 小时。

**无效保温（wasted warming）**：
保温期内会话未回归，心跳花费未兑现任何节省的亏损事件。

### 心跳保活（等待窗保温：已授权实施，Q14 已通过，前缀源为捕获重放——ADR-0006/0007）

**心跳（heartbeat）**：
等待窗口内对主会话缓存前缀的定期体外重放；事件驱动、opt-in、默认关闭，会话恢复即停。
_范围_: 机器等机器（主会话等子代理）；人离开的闲置归摆渡与闸门，不心跳。

**体外重放（external replay）**：
守护进程对捕获快照原样重放、经渡口入站口与真实流量走同一改写路径的保温手段；不写会话文件、聊天界面无感。
_Avoid_: 会话 resume（污染历史，实验报告已否决）、jsonl 重构（已废弃路线，见 ADR-0007）

**捕获重放（capture-replay）**：
心跳前缀的来源方式：透传端口在内存中保留每会话最后一份真实请求体，心跳对其原样重放；字节保真由"重放的就是真实请求"保证。
_Avoid_: jsonl 重构（已废弃路线，见 ADR-0007）

**策略计算器（policy calculator）**：
价格表与实测 TTL 到心跳间隔、划算等待上限、分档矩阵的闭式推导模块；参数不拍脑袋。

**遥测（telemetry）**：
每次心跳实收 cacheRead 构成的线上 TTL 观测；miss 即停跳并告警，建议复测而非自改配置。窗口收尾后主会话首发的实收 cacheRead 是成效判定的地面真值。

**前缀命中（prefix hit）**：
缓存匹配自首个 token 起逐段比对至首个分歧点；请求只要是被缓存内容的开头一段即整段命中，分歧点之后全部重算；不存在倒序、跳段或去头命中。
_Avoid_: 向前命中（方向含糊，只能从头往后比）

**等待上限（wait cap）**：
从派活起愿意花钱保温的最长时长；默认由价格+实测 TTL+该会话前缀大小闭式现算，手动只能调低。到达即停跳、放任过期。

### 问询守望（question watch，已实施，默认关）

**问询守望（question watch）**：
"机器等人"泳道的功能：末条 assistant 回复呈问询潮时，对命中会话开等答复窗口并以问询心跳保温缓存。条件化、会话级、短窗；`[question_watch]` 独立配置节、默认 off，三态永不自动升级；仅 CC 轨，Codex 轨缓行。
_Avoid_: 全局心跳（无条件全局保活仍是红线，见 ADR-0004）、自动保活

**问询潮（question surge）**：
末条 assistant 纯文本的提问密度信号（亦称"提问潮"）：剥代码块后按行归桶计数（❓/**Qn** 标记行、问号行、含问号或疑问词的编号行），独立问题单元 ≥5 判命中；紧判据由实验定型（松判据 163/272 触发且误报全为状态汇报/清单，紧判据 10/272）。
_Avoid_: 提问检测（判定的是密度阈值，不是有无提问）

**等答复窗口（answer window）**：
问询守望命中后挂在会话上的窗口态：窗口内摆渡推迟但带硬死线（拦截阈值前 8 分钟强制入队，失败走既有骨架降级），闸门语义不动；随任何新写入立即关闭。与等待窗口（机器等机器）互为兄弟结构，两窗互斥、先开者赢。
_Avoid_: 保活窗口（心跳是窗口内的手段，不是窗口本身）、暂停闲置（闲置定义不动）

**问询心跳（question-watch beat）**：
等答复窗口内对会话缓存前缀的体外重放保温跳：开窗即排计划，逐跳两道验（无新写入＋转录新鲜度复 stat）后发送，全局同时至多一跳在途；结果按 hit/miss/error 三态加 observe 演练逐跳入既有 beat 科目，连续 miss 自动降级 enforce→observe。真实发送待心跳保真度实验（Q14）放行，observe 演练零网络。
_Avoid_: 心跳（泛称——等待窗保温跳同名，问询侧须加"问询"限定）、会话 resume（污染历史）

**机器等人（machine awaits human）**：
三种等待泳道之一：AI 问出一串问题、用户在组织长答案；归问询守望管。机器等机器归等待窗口，人不在归摆渡与闸门。
_Avoid_: 用户输入中（不可观测——打字草稿不落盘，只有提交才可见）

### 时间线查看器

**时间线（timeline）**：
单会话逐次请求的 token 量与各类事件（派活/心跳/交接/闸门）按时序排布的可视化视图；只读，永不渲染消息内容。

**反跑（backtest）**：
拿历史等待窗口的真实时长与前缀大小、改参数重跑同一套策略公式，得出"若当时这样配 / 实际发生 / 什么都不做"的三线对比。
_Avoid_: 回放（含糊——拖时间游标是另一回事）

**扫参（sweep）**：
在反跑之上把参数网格（间隔、首跳时机、等待上限）与 TTL 场景档位逐一放到历史等待窗口上评分的离线寻优动作；产物是实验报告与输入校准建议，运行时参数仍由策略计算器闭式推导唯一供给——扫参只改公式的输入，不旁路公式。
_Avoid_: 调参（口语，丢掉"在历史数据上评分"语义）、自动调参（暗示改 config，正是被排除的出口）

**演示模式（demo mode）**：
查看器 `--demo` 启动时加载的确定性合成账本——五族系预演心跳全部结局与摆渡全链路；真实心跳行数为零期间的画面预览途径，成本数字由策略公式现算、响应带"演示数据"标注。
_Avoid_: 假数据（贬义且不准——合成但有黄金数依据）

**保活计划（keepalive plan）**：
时序页的策略计算器卡片：给定价格表与实测 TTL 和该会话前缀 S，对假设窗现算 τ、首跳时机、等待上限、最多跳数、单跳与全程成本上限——是"若开启会怎样"的推演，不是已发生的事实。
_Avoid_: 心跳配置（推演不是配置；真执行器仍受三道授权门槛约束）

### agent 面（MCP 只读起步——ADR-0009）

**agent 面（agent surface）**：
Ferryman 暴露给 AI agent 的 MCP 工具面；只读起步，一切读经 daemon 端点，MCP 进程永不直读台账/账本/交接库文件；能力按四格禁区划分，不变量在 daemon 侧代码强制、不写在 agent 提示词。
_Avoid_: API（太泛）、控制接口（v1 无控制）

**降闸禁手（no gate step-down via agent）**：
enforce→observe/off 的闸门降级对 agent 面永不可执行，只能人手改配置或走 CLI；agent 对闸门只有查询与升级建议上报；合法单次绕过只有逃生门两条。
_Avoid_: 权限系统（没有角色与授权链，只有单向禁令）

### 迁移收编（Go 一次性替换）

**公式单源（formula single source）**：
同一策略/价格公式全仓只许一份实现（唯一 Go 策略包），报表、面板、反跑共用；出现第二份实现即缺陷。
_Avoid_: 公式同步（两份靠人工对齐的委婉说法，正是被本条禁止的东西）

**一次性切换（one-shot cutover）**：
Go 版整场战役（守护核心+摆渡执行器+CLI+合并 exe）完工后单点替换 Python：改一行点火脚本，无并行双跑、无影子期、无多日烧机；切换后靠服务日志、账本流水与 /stats 定位修复，回退＝点火脚本指回 Python 存档 tag。
_Avoid_: 灰度、双跑、烧机（本仓语境下指向已废弃的三关门禁，见 ADR-0005）

**规格冻结（spec freeze）**：
迁移期内 Python 侧只收 bugfix 不收新功能；行为规格以既有测试套为唯一基准——内部结构可自由重构，外部行为（闸门判定、账目字段、HTTP 响应、交接文案）不得漂移。
_Avoid_: 双写（新旧两边同时开发同一功能）

### 发布与自升级（2026-09-20 定型）

**发布通道（release channel）**：
正式版本的唯一分发途径：打 tag 触发自动构建产出的 GitHub Release；本地编译只算开发产物，不算发布。
_Avoid_: 发行版（泛）、发版（口语）

**自升级（self-update）**：
同一条 Go 代码线内的版本更换：检查 → 下载校验 → 换 exe → 重启守护；只由人手动触发（CLI 或托盘），agent 面只读、永不触发。
_Avoid_: 自动更新（默认纯手动，无后台自升级）、热更新（exe 是整替的，无热字）

**切换（cutover）**：
Go 与 Python 两个语言栈之间的整体替换/回退机制（`ferryman cutover` 子命令与 rollback-to-python.cmd）；与自升级不共用机制、不混叫——版本轴的升降是自升级，语言栈轴的往返才是切换。
_Avoid_: 一次性切换（特指 2026-09-19 已完成的那场替换，见迁移收编节）、回退（单说含糊）
