# 晨报 2026-09-25 · 余额悬浮窗本机部署（夜链）

## 一句话结论

**悬浮窗已部署并常驻**：登录桌面右缘会看到「Ferryman 用量悬浮窗」，显示智谱 GLM 的 5h 窗剩余环（真查询值）+ 本月 token 估算 + 摆渡次数。daemon 已升级（两次换装，见下），全部测试绿、评审过（1 Blocker 已修、0 Major）。

## 你醒来会看到什么

屏幕**主显示器右缘**竖排小窗（约 136×620，置顶、无任务栏项）：

- **GLM 圆控件**：外环=5h 窗剩余%（当前 99%，重置时刻 11:12 显示倒计时）；圆心 GLM / max；下方「月 9.2G tok估」
- **摆渡圆控件**：「月 577 次 · 周 311 次」（估——摆渡跑在本机 Qwen 上，无价格表，花钱数算不出来就不显示，只显次数）
- 单击圆控件出详情卡（口径说明）；托盘图标可显示/收起/设置；拖动后位置会记住
- daemon 挡住（关掉/重启）时小窗整体灰化加 ⚠，30 秒轮询自愈；daemon 换 token 也自愈

## 部署了什么（全部已提交 main）

| 件 | 内容 | 版本/位置 |
|---|---|---|
| Go daemon | 票06 查询器×3 + 票08 `/widget/summary` 端点 + 票07 月聚合子集 | `v0.2.1-4-g2aa7a12`，commit 7d7c881 + G档修复 |
| widget 壳 | 票09 接线（token 现读/Bearer/8s 超时）+ 首启主屏右缘默认位 | `C:\Users\allan716\AppData\Local\FerrymanWidget\FerrymanWidget.exe` |
| 自启 | HKCU Run 键 `FerrymanWidget` | 登录即拉起（GUI 子系统，零闪窗） |

票面状态：06 ✅ / 07 部分（5h 滚动窗、周 usage、DS 花费计价未做——无 DS 上游无流水，handoff 无价目不造数）/ 08 ✅ / 09 壳侧 ✅。spec 契约枚举同 commit 追加 `handoffs_month/handoffs_week` 两键。

## 对账数字（真机）

- `/widget/summary` month_tokens **9,224,698,644** 与 `/report?scope=month&key=2026-09` 四列合计**背靠背零差**（活账本两调用间的新行会造成 ±几万漂移，属预期）
- 智谱 quota 端点干跑与端点返回一致：percentage=1（已用）→ 剩 99%
- **真机实证**：智谱 max 档只回 TIME_LIMIT + TOKENS_LIMIT(unit:3)，**无 unit:6 周窗**——widget 自动单环+单倒计时（cc-switch 先例同款降级）
- 静态断言 81/81；全仓 `go test ./...` 全绿；评审 1 Blocker（Rust 类型）+ 0 Major + 5 Minor（M1/M4 已修，M2 恢复即拉、M3 数字串 resetTime、M5 硬编码 7311 记录在案）

## 已知事项 / 待办

0. **智谱套餐版本差异已按你补充的口径落地**（commit gc16dd6f）：V1 无周/月限制——悬浮窗只画 5h 环+倒计时，**详情卡注记「V1 套餐语义：无周/月配额窗」**；月度行永远是台账估算值（带"估"角标，非配额窗）。解析侧防错标：带 unit 的响应严格按 unit 锚（3=5h/6=周），未知 unit（如未来 V2+ 月窗）宁可缺失不错标；全无 unit 的老套餐保留启发式。**V2+ 若有月配额窗，unit 码无实证不猜**——升级套餐后抓一次真机响应即可接上。另：真机响应里还有 TIME_LIMIT(unit:5) 工具/时间额度条目（88% 已用，含 search-prime/web-reader 明细），语义未实证暂不展示，要不要上你定。
1. **部署期两发现（都已修）**：①本机是多显示器布局，OS 初放/旧记忆把小窗锚到副屏——默认位已改锚**主屏**右缘；②09-21 开发期存的 window-state.json 记着已不在线的副屏坐标（x=3357，当前单屏 1920 布局下=所有屏之外，小窗会"消失"）——位置恢复已加「必须在某块显示器内」校验，屏外即弃用走默认位，拔屏/换布局不会再丢窗。
2. **`/stats` 的 glm_balance 行显示「余额查询失败：响应解析失败」**——旧票07 字段（打 user/balance），本次未触碰该代码；疑似该端点对编码套餐 key 不适用。悬浮窗不用它（用 quota/limit）。要不要修/删这条旧显示，你定。
3. **旧 /report JSON 非法 `\` 转义**（启用役遗留②）仍在——对账是用正则绕过做的。report 侧待修不变。
4. **旧 daemon 版本号之谜**：换装前跑的是 `v0.1.3-2-g5af93ea`（不在 main 历史里，疑似某次未提交树构建）。现已回正轨 `v0.2.1-6-gc16dd6f`（含版本差异役）。
5. observe 周（至 09-27）说明：本次改动纯增量（新包+新端点），未触闸门/摆渡热路径；daemon 共重启 3 次（G 档、版本差异两役），判据数据源在台账（持久），不受影响。
6. Kimi/DeepSeek：未配置上游/钥（夜里不动 cc-switch DB——需你在场授权）。想点亮：`~/ferryman/config.toml` 的 `[dock.upstreams]` 加条目即可，查询器已就位。
7. widget 自升级（托盘「检查更新」）在签名密钥配好前会如实报错——发布侧人工四件套见 `widget/docs/release.md`，不变。
8. 设置窗里若打开「开机自启」开关，会多写一条插件名自启项（与我手写的 `FerrymanWidget` 键并存，单实例去重，无功能影响）。

## 回滚

- daemon：`mv ferryman.exe.old-20260925 ferryman.exe`（换装前原件，v0.1.3-2-g5af93ea）→ wscript `~/ferryman/start-daemon-hidden.vbs`
- widget：删 Run 键 `FerrymanWidget` + 结束进程即卸；程序本体单文件无残留
- **git 未推送**：main 领先 origin 6 个提交（今晚 4 个），推不推你定

## 复现命令

```
TOKEN=$(cat ~/ferryman/daemon.token)
curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:7311/widget/summary
```
