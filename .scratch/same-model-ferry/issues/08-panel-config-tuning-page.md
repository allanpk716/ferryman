# 票08 · 面板"配置与调参"页(只读)+脱敏全配置端点

## What to build
看得见的收口,打通端到端:daemon 新增只读端点——返回脱敏全配置(密钥一律打码,任何 *_key 字段不出现明文)与调参三列数据:配置值(手编)/ 生效值(计算器现算,经票05 出口)/ 建议值(经票06 报告,带状态:待审/已接受/已拒绝/已自动应用),**按上游分列**,无观测上游显示"无建议"。面板(15900,internal/viewer)新增"配置与调参"页:全配置总览分区渲染+三列对比表+状态徽章,三列分列显示禁止混排(照 widget 查询值/估算值的分列规矩);纯只读,页面上没有任何写操作按钮。

## 验收标准
- [ ] 端点脱敏单测:任何 api_key 字段不出现在响应(断言)
- [ ] 三列数据按上游分列,无观测上游"无建议",单测覆盖
- [ ] 面板新页渲染三列+徽章,只读(无按钮/无 POST)
- [ ] 密钥打码在页面上以掩码呈现(如 sk-****)
- [ ] 既有面板/端点测试照绿

## Blocked by
票07

## 涉及路径
internal/daemon/
internal/viewer/

## 副作用声明
go test ./internal/daemon/... ./internal/viewer/...;go build ./...

## decision_refs
D12 D11

## review_blocks
无
